# ---------------------------------------------------------------------------
# NEXT_PUBLIC_API_URL — Backend host for API calls
# ---------------------------------------------------------------------------
# Set to the HOST ROOT ONLY. Do NOT include "/api" at the end.
# The frontend code adds "/api/..." paths automatically on every request.
# Including "/api" here would create doubled paths like /api/api/tenants → 404.
#
# Local development (dotnet run):
NEXT_PUBLIC_API_URL=http://localhost:5090
#
# Production / single-host deployment (ASP.NET backend serves the frontend too):
# Leave EMPTY so the browser uses same-origin relative paths (/api/...).
# NEXT_PUBLIC_API_URL=
#
# Cross-origin deployment (frontend on CDN, backend on separate host):
# NEXT_PUBLIC_API_URL=https://api.myapp.com       ← host only, no /api suffix
# ---------------------------------------------------------------------------
